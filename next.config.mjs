/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['greatertechhub.com'], // Add your domain here
    },
  };
  
  export default nextConfig;
  
