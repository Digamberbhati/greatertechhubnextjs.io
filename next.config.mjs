/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['greatertechhub.com', 'www.systemart.com','assets.aceternity.com','images.unsplash.com'], // Add your domain here
    },
  };
  
  export default nextConfig;
  
